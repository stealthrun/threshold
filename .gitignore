# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Eval run outputs (regenerated)
evals/results/*
!evals/results/.gitkeep

# Secrets / env — never commit
.env
.env.*
# A real config holds the API key in plaintext; it lives in ~/.config, but guard the repo
# in case one is created here. The committed template is config.example.toml.
config.toml
config.json
!config.example.toml

# OS / editor
.DS_Store
.idea/
.vscode/

# Local agent guide — never committed
CLAUDE.md
